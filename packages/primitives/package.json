{
  "name": "@homunculus/primitives",
  "version": "0.0.1",
  "description": "Built-in primitive agents for homunculus (vision, memory, reasoning, action).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/osmanorhanhomunculus.git",
    "directory": "packages/primitives"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc --build --clean",
    "lint": "echo \"lint not yet configured\"",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@homunculus/core": "workspace:*",
    "@homunculus/semantic-engine": "workspace:*"
  },
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  }
}
